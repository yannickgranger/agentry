# listener-agent — reads team_context from startup JSON; echoes received messages.
FROM alpine:3.21
RUN apk add --no-cache coreutils bash jq
COPY entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh
ENTRYPOINT ["/entrypoint.sh"]
