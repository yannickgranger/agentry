//! orchestrator-dashboard — placeholder; filled in M1.
//!
//! The dashboard is deliberately empty until M1 (dashboard + SSE). M0 ships
//! the runtime + echo-agent without a UI so the verify loop is purely
//! brief-in / verdict-out on Redis.

fn main() {
    eprintln!("orchestrator-dashboard: not yet implemented (M1).");
    std::process::exit(0);
}
