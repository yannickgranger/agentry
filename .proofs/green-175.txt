issue #175 GREEN proof — fix applied, reviewer-claude-runner now loads and emits events

Fix: crates/orchestrator-runtime/src/seed.rs
  Bumped 4 role builders from `debian:bookworm-slim` (glibc 2.36) to
  `debian:trixie-slim` (glibc 2.41) via a new `RUNNER_HOST_IMAGE`
  constant. Roles: reviewer-claude-agentry, ac-verifier-claude-agentry,
  ac-verifier-gemini-agentry, ac-verifier-grok-agentry.

Unit test (cargo test):

    $ cargo test -p orchestrator-runtime --lib issue_175
    running 1 test
    test seed::tests::issue_175_runner_host_roles_use_glibc_compatible_image ... ok
    test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 151 filtered out

End-to-end re-dispatch (after rebuild + dev-down/dev-up from the fix worktree):

    $ ./target/release/orchestrator submit /tmp/brf_verify_175_v2.json
    {"submitted":true,"brief_id":"brf_verify_175_glibc_v2","stream_id":"1777461368225-0"}

Reviewer container's stderr (from /tmp/agentry-orchestratord.log):

    debconf: unable to initialize frontend: Dialog
    debconf: (TERM is not set, so the dialog frontend is not usable.)
    debconf: falling back to frontend: Readline

  ↑ Only debconf noise. NO `version 'GLIBC_X.Y' not found` line. The
    binary loaded.

Reviewer trace events emitted (proves DoneGuard registered + main ran):

    "msg":"calling claude -p"           ← coder
    "msg":"claude reply received"        ← coder
    "msg":"v1+ topology — skipping..."   ← coder bash exitpoint
    "msg":"workspace worktree ready"     ← coder
    "error":"empty diff — coder produced no changes"  ← reviewer-runner

  ↑ The "empty diff" event is the reviewer-claude-runner's own
    diff-emptiness check at lines 113-123 of reviewer_claude_runner.rs.
    Its emission confirms the binary loaded, DoneGuard registered, and
    the runner reached the workspace+diff inspection stage. The
    diff-emptiness verdict is correct — this verification brief targets
    an issue whose work was already in `develop` (see #169 retry2).

Spawn confirmed using the new image:

    spawning container brief=brf_verify_175_glibc_v2
                       role=reviewer-claude-agentry
                       image=docker.io/library/debian:trixie-slim

Reviewer role definition in Redis after seed:

    $ redis-cli ... GET agentry:role:reviewer-claude-agentry:v1 | jq -r .image
    docker.io/library/debian:trixie-slim

The original failure mode (silent exit, no events, synthesized "agent
exited without done event" verdict) is structurally closed. Whatever
the reviewer's verdict ends up being for any given brief, the runner
loads, runs, and emits events.
