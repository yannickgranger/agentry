issue #175 RED proof — substrate-level GLIBC mismatch

Captured 2026-04-29 from orchestratord running on origin/develop tip
(2a86d44, before this fix), brief brf_diag_175_reviewer_runner_silent
dispatched at 10:57:30Z.

Diagnostic command (with RUST_LOG=orchestrator_runtime=debug,info on
the daemon so spawner.rs:586's tracing::debug! stderr capture
becomes visible):

    just dev-down && \
    RUST_LOG="orchestrator_runtime=debug,info" \
    GITEA_TOKEN=<from-kdbx> \
    just dev-up

    ./target/release/orchestrator submit /tmp/brf_diag_175.json
    # then tail -f /tmp/agentry-orchestratord.log

Result — reviewer container's stderr (captured in orchestratord log):

    debconf: delaying package configuration, since apt-utils is not installed
    /usr/local/bin/reviewer-claude-runner: /lib/x86_64-linux-gnu/libc.so.6: version `GLIBC_2.39' not found (required by /usr/local/bin/reviewer-claude-runner)

Brief verdict:

    {"brief":"brf_diag_175_reviewer_runner_silent","kind":"failed",
     "reason":"agent exited without done event (code=Some(1))"}

Reviewer trace events: 0 (only the spawner's spawn + terminated envelopes).

Cause confirmed:

  $ podman run --rm docker.io/library/debian:bookworm-slim ldd --version | head -1
  ldd (Debian GLIBC 2.36-9+deb12u13) 2.36

  $ objdump -T ~/.local/bin/reviewer-claude-runner | grep -oE 'GLIBC_[0-9.]+' | sort -V -u | tail -1
  GLIBC_2.39

The dynamic linker rejects the binary before main() runs, so DoneGuard
never registers and no events emit. The container exits with the
linker's error code (1) and the spawner falls back to its synthesized
"agent exited without done event" verdict.

Compare: coder-claude-agentry uses rust:1.93 (Debian trixie base, glibc
2.41) and runs the same-class runner binary — works.
